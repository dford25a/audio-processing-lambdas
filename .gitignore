# Terraform generated files and directories
.terraform/
.terraform.lock.hcl
*.tfstate
*.tfstate.*.backup
*.tfvars
terraform.tfstate.d/

# Specific Terraform state files (redundant with above, but explicit for clarity)
terraform/application/terraform.tfstate
terraform/application/terraform.tfstate.*.backup
terraform/application/terraform.tfstate.backup
terraform/shared/terraform.tfstate
terraform/shared/terraform.tfstate.backup

# Generated zip archives (Lambda deployment packages)
*.zip

# FFmpeg binaries and archives
segment-audio-container/ffmpeg-7.0.2-amd64-static/
segment-audio-container/ffmpeg-release-amd64-static.tar.xz

# Other specific files
default_state.json
import_files.sh